# Contact form (Web3Forms). Create your own key at https://web3forms.com
# Note: do not commit your real key here — it stays server-side only
# (read by src/app/api/contact/route.ts) and is never exposed to the client.
WEB3FORMS_ACCESS_KEY=your_web3forms_access_key
